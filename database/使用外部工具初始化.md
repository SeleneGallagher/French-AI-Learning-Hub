# 🔧 使用外部数据库工具初始化数据库

如果 Railway CLI 方法不工作，可以使用外部数据库工具。

## 📋 步骤

### 1. 获取数据库连接信息

在 Railway Dashboard 中：

1. 点击 PostgreSQL 服务
2. 点击 **"Connect"** 按钮（右上角）
3. 会显示连接信息，类似：
   ```
   Host: xxxxx.railway.app
   Port: 5432
   Database: railway
   User: postgres
   Password: xxxxx
   ```
   或者直接显示连接字符串：
   ```
   postgresql://postgres:password@host:port/railway
   ```

### 2. 选择数据库工具

推荐工具（任选一个）：

#### DBeaver（免费，推荐）⭐
- 下载：https://dbeaver.io/download/
- 支持 Windows/Mac/Linux
- 功能强大，完全免费

#### TablePlus（界面美观）
- 下载：https://tableplus.com/
- 支持 Windows/Mac
- 有免费版和付费版

#### pgAdmin（PostgreSQL 官方工具）
- 下载：https://www.pgadmin.org/download/
- 完全免费
- 功能全面但界面较复杂

### 3. 连接数据库

以 **DBeaver** 为例：

1. 打开 DBeaver
2. 点击 **"New Database Connection"**（左上角）
3. 选择 **PostgreSQL**
4. 填写连接信息：
   - **Host**: Railway 提供的 Host
   - **Port**: 5432（或 Railway 提供的端口）
   - **Database**: railway（或 Railway 提供的数据库名）
   - **Username**: postgres（或 Railway 提供的用户名）
   - **Password**: Railway 提供的密码
5. 点击 **"Test Connection"** 测试连接
6. 如果成功，点击 **"Finish"**

### 4. 执行 SQL 脚本

1. 在 DBeaver 中，右键点击数据库连接
2. 选择 **"SQL Editor"** → **"New SQL Script"**
3. 打开项目中的 `database/init.sql` 文件
4. 复制所有内容（Ctrl+A, Ctrl+C）
5. 粘贴到 SQL 编辑器中（Ctrl+V）
6. 点击 **"Execute SQL Script"** 按钮（或按 F5）
7. 等待执行完成，应该会显示 "Success" 或类似消息

### 5. 验证初始化

在 DBeaver 中：

1. 展开数据库连接
2. 展开 **"Schemas"** → **"public"** → **"Tables"**
3. 应该能看到以下表：
   - users
   - registration_codes
   - dict_history
   - dict_favorites
   - vocab_progress
   - expressions
   - expression_favorites
   - ai_chat_history
   - movie_watchlist

### 6. 验证注册码

执行以下 SQL 查询：

```sql
SELECT code, unlimited_use, is_active 
FROM registration_codes;
```

应该能看到：
```
code           | unlimited_use | is_active
---------------|---------------|----------
202300210001   | true          | true
```

---

## ✅ 完成！

数据库初始化完成后，回到 Railway Dashboard，继续下一步操作。

---

**如果遇到问题：**
- 检查连接信息是否正确
- 确认 PostgreSQL 服务状态为 "Online"
- 检查防火墙设置（Railway 数据库通常需要 SSL 连接）

