# 环境变量配置示例文件
# 复制此文件为 .env 并填写实际值
# ⚠️ 注意：.env 文件不会被提交到 Git

# ============================================
# 🔴 必需的环境变量
# ============================================

# PostgreSQL 数据库连接 URL（Railway 自动提供）
# 格式: postgresql://user:password@host:port/database
# 在 Railway 中，添加 PostgreSQL 服务后会自动设置此变量
DATABASE_URL=postgresql://user:password@host:port/database

# JWT 签名密钥（用于用户认证）
# 生成方式：
#   Linux/Mac: openssl rand -hex 32
#   Windows PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})
#   Python: import secrets; secrets.token_hex(32)
# ⚠️ 生产环境必须更改默认值！
JWT_SECRET=your-secret-key-change-this

# ============================================
# 🟡 可选的环境变量
# ============================================

# JWT 签名密钥（用于用户认证）
# 生成方式：
#   Linux/Mac: openssl rand -hex 32
#   Windows PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})
#   Python: import secrets; secrets.token_hex(32)
# ⚠️ 生产环境必须更改默认值！
JWT_SECRET=your-secret-key-change-this

# Coze 机器人 ID（AI 助手功能）
# 获取方式：Coze 平台 → 机器人设置 → Bot ID
COZE_BOT_ID=your-coze-bot-id

# Coze 访问令牌（PAT - Personal Access Token）
# 获取方式：Coze 平台 → 开发者设置 → 创建 PAT Token
COZE_PAT_TOKEN=your-coze-pat-token

# Coze 系统令牌（SAT - System Access Token，备选）
# 如果使用 SAT Token 而不是 PAT Token，可以设置这个
# 注意：COZE_PAT_TOKEN 和 COZE_SAT_TOKEN 二选一即可
# 代码会优先使用 PAT_TOKEN，如果没有则使用 SAT_TOKEN
COZE_SAT_TOKEN=your-coze-sat-token

# DeepSeek API 密钥（用于新闻关键词、语用表达生成等）
# 获取方式：https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=your-deepseek-api-key

# TMDB API 密钥（用于电影数据和海报）
# 获取方式：https://www.themoviedb.org/settings/api
TMDB_API_KEY=your-tmdb-api-key

# 管理员密码（用于访问管理员面板查看所有用户信息）
# ⚠️ 生产环境必须设置强密码！
ADMIN_PASSWORD=your-admin-password-change-this


