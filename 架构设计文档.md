# French AI Learning Hub - 前后端架构设计文档

## 📋 目录
1. [整体架构](#整体架构)
2. [数据库设计](#数据库设计)
3. [后端API设计](#后端api设计)
4. [前端模块设计](#前端模块设计)
5. [用户认证系统](#用户认证系统)
6. [数据迁移方案](#数据迁移方案)

---

## 整体架构

### 架构图
```
┌─────────────────┐
│   前端 (Browser) │
│  - React/Vanilla │
│  - LocalStorage  │ (临时缓存)
└────────┬─────────┘
         │ HTTP/HTTPS
         │ JSON API
┌────────▼─────────┐
│   后端服务器      │
│  - Flask/FastAPI  │
│  - SQLite/MySQL  │
│  - 代理所有API    │
└────────┬─────────┘
         │
    ┌────┴────┬──────────┬──────────┐
    │         │          │          │
┌───▼───┐ ┌──▼───┐ ┌───▼───┐ ┌───▼───┐
│新闻RSS│ │TMDB  │ │DeepSeek│ │ Coze  │
│源     │ │API   │ │ API    │ │ API   │
└───────┘ └──────┘ └────────┘ └───────┘
```

### 技术栈
- **后端**: Python Flask/FastAPI
- **数据库**: SQLite (开发) / MySQL/PostgreSQL (生产)
- **前端**: 原生 JavaScript (ES6+)
- **认证**: JWT Token
- **密码加密**: bcrypt

---

## 数据库设计

### 1. 用户表 (users)
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_login TIMESTAMP,
    is_active BOOLEAN DEFAULT 1
);
```

### 2. 词典查询历史 (dict_history)
```sql
CREATE TABLE dict_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    word VARCHAR(100) NOT NULL,
    searched_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_user_word (user_id, word)
);
```

### 3. 词典收藏 (dict_favorites)
```sql
CREATE TABLE dict_favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    word VARCHAR(100) NOT NULL,
    word_data TEXT,  -- JSON格式存储完整单词信息
    favorited_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE(user_id, word)
);
```

### 4. 背单词进度 (vocab_progress)
```sql
CREATE TABLE vocab_progress (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    word VARCHAR(100) NOT NULL,
    quality INTEGER DEFAULT 0,  -- 0:生疏, 1:模糊, 2:熟练
    review_count INTEGER DEFAULT 0,
    last_review TIMESTAMP,
    next_review TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE(user_id, word)
);
```

### 5. 语用表达记录 (expressions)
```sql
CREATE TABLE expressions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    scenario TEXT NOT NULL,  -- 用户输入的场景
    expression_data TEXT NOT NULL,  -- JSON格式存储生成的表达
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

### 6. 语用表达收藏 (expression_favorites)
```sql
CREATE TABLE expression_favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    expression_id INTEGER NOT NULL,
    favorited_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (expression_id) REFERENCES expressions(id) ON DELETE CASCADE,
    UNIQUE(user_id, expression_id)
);
```

### 7. AI助手聊天记录 (ai_chat_history)
```sql
CREATE TABLE ai_chat_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    conversation_id VARCHAR(100),  -- Coze平台的会话ID
    role VARCHAR(10) NOT NULL,  -- 'user' or 'assistant'
    content TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_user_conversation (user_id, conversation_id)
);
```

### 8. 影视收藏 (movie_favorites)
```sql
CREATE TABLE movie_favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    movie_id INTEGER NOT NULL,  -- TMDB电影ID
    movie_data TEXT,  -- JSON格式存储电影信息
    added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE(user_id, movie_id)
);
```

### 9. 注册码表 (registration_codes)
```sql
CREATE TABLE registration_codes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code VARCHAR(50) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    used_at TIMESTAMP,
    used_by_user_id INTEGER,
    is_active BOOLEAN DEFAULT 1,
    FOREIGN KEY (used_by_user_id) REFERENCES users(id)
);
```

---

## 后端API设计

### 基础URL
```
/api/v1
```

### 认证相关

#### 1. 用户注册
```
POST /api/v1/auth/register
Request Body:
{
    "username": "string",
    "password": "string",
    "registration_code": "string"
}
Response:
{
    "success": true,
    "message": "注册成功",
    "token": "jwt_token",
    "user": {
        "id": 1,
        "username": "string"
    }
}
```

#### 2. 用户登录
```
POST /api/v1/auth/login
Request Body:
{
    "username": "string",
    "password": "string"
}
Response:
{
    "success": true,
    "token": "jwt_token",
    "user": {
        "id": 1,
        "username": "string"
    }
}
```

#### 3. 验证Token
```
GET /api/v1/auth/verify
Headers: Authorization: Bearer {token}
Response:
{
    "success": true,
    "user": {
        "id": 1,
        "username": "string"
    }
}
```

### 词典模块

#### 1. 获取查询历史
```
GET /api/v1/dictionary/history?limit=50
Headers: Authorization: Bearer {token}
Response:
{
    "success": true,
    "data": [
        {
            "id": 1,
            "word": "bonjour",
            "searched_at": "2024-01-01T10:00:00Z"
        }
    ]
}
```

#### 2. 添加查询历史
```
POST /api/v1/dictionary/history
Headers: Authorization: Bearer {token}
Request Body:
{
    "word": "bonjour"
}
```

#### 3. 获取收藏列表
```
GET /api/v1/dictionary/favorites
Headers: Authorization: Bearer {token}
```

#### 4. 添加收藏
```
POST /api/v1/dictionary/favorites
Headers: Authorization: Bearer {token}
Request Body:
{
    "word": "bonjour",
    "word_data": {...}  // 完整单词信息
}
```

#### 5. 删除收藏
```
DELETE /api/v1/dictionary/favorites/{id}
Headers: Authorization: Bearer {token}
```

#### 6. 获取背单词进度
```
GET /api/v1/dictionary/vocab-progress
Headers: Authorization: Bearer {token}
```

#### 7. 更新背单词进度
```
PUT /api/v1/dictionary/vocab-progress
Headers: Authorization: Bearer {token}
Request Body:
{
    "word": "bonjour",
    "quality": 2,  // 0/1/2
    "review_count": 5
}
```

### 语用模块

#### 1. 获取表达记录
```
GET /api/v1/expressions?limit=50
Headers: Authorization: Bearer {token}
```

#### 2. 创建表达记录
```
POST /api/v1/expressions
Headers: Authorization: Bearer {token}
Request Body:
{
    "scenario": "在咖啡馆点餐",
    "expression_data": {...}  // AI生成的表达
}
```

#### 3. 获取收藏的表达
```
GET /api/v1/expressions/favorites
Headers: Authorization: Bearer {token}
```

#### 4. 收藏/取消收藏表达
```
POST /api/v1/expressions/{id}/favorite
DELETE /api/v1/expressions/{id}/favorite
Headers: Authorization: Bearer {token}
```

### AI助手模块

#### 1. 获取聊天历史
```
GET /api/v1/ai/chat-history?conversation_id={id}&limit=50
Headers: Authorization: Bearer {token}
```

#### 2. 发送消息（通过后端代理）
```
POST /api/v1/ai/chat
Headers: Authorization: Bearer {token}
Request Body:
{
    "message": "你好",
    "conversation_id": "optional"
}
Response: (流式响应)
```

#### 3. 清除聊天历史
```
DELETE /api/v1/ai/chat-history
Headers: Authorization: Bearer {token}
Request Body:
{
    "conversation_id": "optional"  // 不传则清除所有
}
```

### 新闻模块（代理）

#### 1. 获取新闻列表
```
GET /api/v1/news
Response:
{
    "success": true,
    "data": [
        {
            "title": "...",
            "link": "...",
            "description": "...",
            "source": "France 24",
            "published_at": "..."
        }
    ]
}
```
后端会从RSS源抓取，避免CORS问题

#### 2. 获取新闻关键词（通过DeepSeek）
```
POST /api/v1/news/keywords
Headers: Authorization: Bearer {token}
Request Body:
{
    "article_text": "..."
}
```

### 影视模块（代理）

#### 1. 获取影视列表
```
GET /api/v1/movies?page=1
Response:
{
    "success": true,
    "data": [...]
}
```
后端从TMDB API获取

#### 2. 获取影视收藏
```
GET /api/v1/movies/favorites
Headers: Authorization: Bearer {token}
```

#### 3. 添加/删除影视收藏
```
POST /api/v1/movies/favorites
DELETE /api/v1/movies/favorites/{movie_id}
Headers: Authorization: Bearer {token}
```

---

## 前端模块设计

### 1. 用户认证模块 (auth.js)

```javascript
// scripts/services/auth.js
export class AuthService {
    static getToken() {
        return localStorage.getItem('auth_token');
    }
    
    static setToken(token) {
        localStorage.setItem('auth_token', token);
    }
    
    static removeToken() {
        localStorage.removeItem('auth_token');
    }
    
    static async login(username, password) {
        const response = await fetch('/api/v1/auth/login', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ username, password })
        });
        const data = await response.json();
        if (data.success) {
            this.setToken(data.token);
            return data.user;
        }
        throw new Error(data.message);
    }
    
    static async register(username, password, registrationCode) {
        // 类似实现
    }
    
    static async verifyToken() {
        // 验证token有效性
    }
}
```

### 2. API服务封装 (apiService.js)

```javascript
// scripts/services/apiService.js
import { AuthService } from './auth.js';

export class APIService {
    static async request(url, options = {}) {
        const token = AuthService.getToken();
        const headers = {
            'Content-Type': 'application/json',
            ...options.headers
        };
        
        if (token) {
            headers['Authorization'] = `Bearer ${token}`;
        }
        
        const response = await fetch(`/api/v1${url}`, {
            ...options,
            headers
        });
        
        if (response.status === 401) {
            // Token过期，跳转登录
            AuthService.removeToken();
            window.location.hash = '#login';
            throw new Error('未授权，请重新登录');
        }
        
        return response.json();
    }
    
    // 词典相关
    static async getDictHistory() {
        return this.request('/dictionary/history');
    }
    
    static async addDictHistory(word) {
        return this.request('/dictionary/history', {
            method: 'POST',
            body: JSON.stringify({ word })
        });
    }
    
    // ... 其他API方法
}
```

### 3. 登录/注册模块 (login.js)

```javascript
// scripts/modules/login.js
export function initLogin() {
    // 显示登录界面
    // 处理登录/注册表单
    // 成功后加载用户数据
}
```

### 4. 关于模块 (about.js)

```javascript
// scripts/modules/about.js
export function initAbout() {
    // 显示项目信息
    // 列出所有使用的API和资源
}
```

---

## 用户认证系统

### JWT Token流程
1. 用户登录/注册 → 后端验证 → 返回JWT Token
2. 前端存储Token到localStorage
3. 每次API请求携带Token: `Authorization: Bearer {token}`
4. 后端验证Token有效性
5. Token过期 → 前端清除Token → 跳转登录页

### 注册码机制
- 管理员在数据库中预置注册码
- 用户注册时输入注册码
- 后端验证注册码是否存在且未使用
- 验证通过后创建用户，标记注册码为已使用

---

## 数据迁移方案

### 从LocalStorage迁移到后端
1. **检测登录状态**
   - 如果已登录，在首次加载时同步数据
   
2. **迁移脚本**
```javascript
async function migrateLocalDataToServer() {
    // 1. 词典历史
    const localHistory = JSON.parse(localStorage.getItem('dict_history') || '[]');
    for (const item of localHistory) {
        await APIService.addDictHistory(item.word);
    }
    
    // 2. 词典收藏
    const localFavorites = JSON.parse(localStorage.getItem('dict_favorites') || '[]');
    for (const fav of localFavorites) {
        await APIService.addDictFavorite(fav);
    }
    
    // 3. 背单词进度
    const localProgress = JSON.parse(localStorage.getItem('vocab_progress') || '{}');
    for (const [word, progress] of Object.entries(localProgress)) {
        await APIService.updateVocabProgress(word, progress);
    }
    
    // 4. 语用记录
    const localExpressions = await getAllExpressions();
    for (const expr of localExpressions) {
        await APIService.createExpression(expr);
    }
    
    // 5. AI聊天记录
    const localChat = localStorageService.get('ai_chat_history', []);
    for (const msg of localChat) {
        await APIService.saveChatMessage(msg);
    }
    
    // 迁移完成后清除本地数据（可选）
    // localStorage.clear();
}
```

---

## 实施步骤

### 阶段1: 后端基础架构
1. 创建Flask/FastAPI项目
2. 配置数据库（SQLite）
3. 实现用户认证（注册/登录/JWT）
4. 实现注册码验证

### 阶段2: 后端API实现
1. 词典模块API
2. 语用模块API
3. AI助手API（代理Coze）
4. 新闻/影视代理API

### 阶段3: 前端改造
1. 创建登录/注册界面
2. 创建关于模块
3. 改造现有模块使用后端API
4. 实现数据迁移

### 阶段4: 测试和优化
1. 功能测试
2. 性能优化
3. 安全性检查

---

## 关于模块内容示例

```markdown
# 关于 French AI Learning Hub

## 项目简介
一个功能丰富的法语学习平台...

## 使用的网络资源

### 新闻模块
- **France 24**: https://www.france24.com/fr/rss
- **Le Monde**: https://www.lemonde.fr/rss/une.xml
- **RFI**: https://www.rfi.fr/fr/rss
- **franceinfo**: https://www.franceinfo.fr/rss
- **20 Minutes**: https://www.20minutes.fr/rss/une.xml

### 影视模块
- **TMDB API**: https://www.themoviedb.org/
  - 用于获取法语电影和剧集信息
  - 包括海报、评分、简介等

### AI服务
- **DeepSeek API**: https://platform.deepseek.com/
  - 用于新闻关键词提取
  - 用于语用表达生成
  
- **Coze API**: https://www.coze.cn/
  - 用于AI助手对话功能
  - 提供流式响应

### 词典数据
- **French-Dictionary**: https://github.com/hbenbel/French-Dictionary
  - 基于Wiktionary的完整法语词典

## 技术栈
- 前端: JavaScript (ES6+), Tailwind CSS
- 后端: Python Flask/FastAPI
- 数据库: SQLite/MySQL
- 认证: JWT Token
```

---

## 注意事项

1. **安全性**
   - 密码使用bcrypt加密
   - JWT Token设置合理过期时间
   - API请求频率限制
   - SQL注入防护

2. **性能**
   - 数据库索引优化
   - API响应缓存
   - 分页加载数据

3. **用户体验**
   - 登录状态持久化
   - 数据自动同步
   - 离线提示

